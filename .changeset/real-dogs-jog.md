---
---

Monorepo restructuring only (repo layout, tooling, docs). No package's published
output changed — verified via `npm pack --dry-run` diff against each package's
pre-migration tarball. No version bump needed.
