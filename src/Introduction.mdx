import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Introduction" />

# React Action Guard

**Coordinate UI blocking across async actions and competing interactions in React**

React Action Guard helps you prevent duplicate submits, conflicting actions, and stale UI interactions without scattering local `isLoading` flags across components.

## Start Here

- Start with `useAsyncAction` when you need to block during async work
- Use `scope` to coordinate blocking across unrelated components
- Use `useIsBlocked` and `useBlockingInfo` to react to active blockers
- Add `UIBlockingProvider` when you need isolated state for SSR, tests, or micro-frontends
- Reach for advanced hooks only when you need scheduled, confirmable, or polling-based flows

## Core Concepts

### Blockers

A **blocker** is a declarative record that says some part of the UI should currently be treated as blocked. Each blocker has:

- **ID** - Unique identifier
- **Scope** - Which part of the UI to block (`global`, `form`, `payment`, etc.)
- **Reason** - Human-readable explanation
- **Priority** - Determines precedence when multiple blockers are active

### Scopes

**Scopes** allow you to block specific parts of your application independently:

```tsx
// Block only payment-related UI
useBlocker("payment-processing", { scope: "payment" }, isProcessing);

// Block everything
useBlocker("maintenance", { scope: "global" }, isMaintenanceMode);

// Block multiple scopes
useBlocker("multi-scope", { scope: ["form", "buttons"] }, isActive);
```

## Core Workflow

For most apps, the path looks like this:

1. Wrap async work with `useAsyncAction`
2. Use a shared `scope`
3. Disable or annotate related UI with `useIsBlocked` or `useBlockingInfo`

## Hooks Overview

### Core hooks

| Hook              | Purpose                 | Typical use case                  |
| ----------------- | ----------------------- | --------------------------------- |
| `useAsyncAction`  | Wrap async operations   | API calls, mutations, submissions |
| `useBlocker`      | Manual blocking control | Existing boolean state            |
| `useIsBlocked`    | Check blocking state    | Disable buttons, show loaders     |
| `useBlockingInfo` | Read blocker metadata   | Tooltips, banners, debug UI       |

### Advanced hooks

| Hook                    | Purpose                      | Typical use case                      |
| ----------------------- | ---------------------------- | ------------------------------------- |
| `useConfirmableBlocker` | Confirmation flow helper     | Destructive actions                   |
| `useScheduledBlocker`   | Time-based blocking          | Maintenance windows                   |
| `useConditionalBlocker` | Polling-based blocking sync  | External state without event hooks    |

## 🚀 Quick Start

### Installation

```bash
npm install @okyrychenko-dev/react-action-guard zustand
```

### Basic Example

```tsx
import { useAsyncAction, useIsBlocked } from "@okyrychenko-dev/react-action-guard";

function SaveButton() {
  const isBlocked = useIsBlocked("form");
  const saveWithBlocking = useAsyncAction("save-form", "form");

  const handleSave = async () => {
    await saveWithBlocking(async () => {
      await api.saveData();
    });
  };

  return (
    <button onClick={handleSave} disabled={isBlocked}>
      {isBlocked ? "Saving..." : "Save"}
    </button>
  );
}
```

## Common Patterns

### Form Submission

```tsx
function MyForm() {
  const isBlocked = useIsBlocked("form");
  const submitForm = useAsyncAction("submit-form", "form");

  const handleSubmit = async (data) => {
    await submitForm(async () => {
      await api.submit(data);
    });
  };

  return (
    <form onSubmit={handleSubmit}>
      <input disabled={isBlocked} />
      <button type="submit" disabled={isBlocked}>
        Submit
      </button>
    </form>
  );
}
```

### Cross-Component Coordination

```tsx
function SaveButton() {
  const runSave = useAsyncAction("save-form", "form");
  const isBlocked = useIsBlocked("form");

  return (
    <button disabled={isBlocked} onClick={() => runSave(() => api.saveData())}>
      Save
    </button>
  );
}

function FormStatus() {
  const blockers = useBlockingInfo("form");
  return blockers[0] ? <p>Blocked: {blockers[0].reason}</p> : null;
}
```

### Provider Isolation

```tsx
function App() {
  return (
    <UIBlockingProvider>
      <MyApp />
    </UIBlockingProvider>
  );
}
```

### Advanced: Confirmation Dialogs

```tsx
function DeleteButton({ itemId }) {
  const { execute, isDialogOpen, confirmConfig, onConfirm, onCancel } = useConfirmableBlocker(
    "delete-item",
    {
      scope: "items",
      confirmMessage: "Are you sure you want to delete this item?",
      onConfirm: async () => {
        await api.deleteItem(itemId);
      },
    }
  );

  return (
    <>
      <button onClick={execute}>Delete</button>

      {isDialogOpen && (
        <ConfirmDialog
          title={confirmConfig.title}
          message={confirmConfig.message}
          onConfirm={onConfirm}
          onCancel={onCancel}
        />
      )}
    </>
  );
}
```

### Advanced: Scheduled Maintenance

```tsx
function App() {
  const isBlocked = useIsBlocked("global");

  useScheduledBlocker("maintenance", {
    scope: "global",
    reason: "Scheduled maintenance in progress",
    schedule: {
      start: new Date("2024-01-01T02:00:00"),
      duration: 30 * 60 * 1000, // 30 minutes
    },
    onScheduleStart: () => {
      toast.info("Maintenance started");
    },
    onScheduleEnd: () => {
      toast.success("Maintenance completed");
    },
  });

  return (
    <div>
      {isBlocked && <MaintenanceBanner />}
      <YourApp />
    </div>
  );
}
```

### Advanced: Conditional Validation

```tsx
function PaymentForm() {
  const [amount, setAmount] = useState(0);
  const isBlocked = useIsBlocked("payment");

  useConditionalBlocker("amount-validation", {
    scope: "payment",
    reason: "Amount exceeds limit",
    condition: () => amount > 10000,
  });

  return (
    <div>
      <input type="number" value={amount} onChange={(e) => setAmount(Number(e.target.value))} />
      <button disabled={isBlocked}>Pay ${amount}</button>
      {isBlocked && <ErrorMessage>Amount exceeds $10,000 limit</ErrorMessage>}
    </div>
  );
}
```

## Optional Extensions

### Priority System

When multiple blockers are active, the highest priority blocker takes precedence:

```tsx
useBlocker("low-priority", { scope: "app", priority: 10 }, true);
useBlocker("high-priority", { scope: "app", priority: 90 }, true);
// high-priority blocker's reason will be shown
```

### Middleware

Middleware is optional. Use it when you need observability or analytics around blocker lifecycle events:

```tsx
import { uiBlockingStoreApi } from "@okyrychenko-dev/react-action-guard";

const loggingMiddleware = (context) => {
  console.log(`Blocker ${context.action}: ${context.blockerId}`);
};

uiBlockingStoreApi.getState().registerMiddleware("logger", loggingMiddleware);
```

### Multiple Scopes

Check or block multiple scopes simultaneously:

```tsx
// Block multiple scopes
useBlocker("multi", { scope: ["form", "buttons", "inputs"] }, isActive);

// Check if any scope is blocked
const isAnyBlocked = useIsBlocked(["scope1", "scope2", "scope3"]);
```

## Next Steps

- Start with `useAsyncAction`, `useBlocker`, `useIsBlocked`, and `useBlockingInfo`
- Explore advanced hooks only when your flow needs them
- Use the sidebar for interactive examples and detailed API docs

## Contributing

Contributions are welcome! Visit my [GitHub repository](https://github.com/okyrychenko-dev/react-action-guard) to report issues or submit pull requests.

## License

MIT © [Oleksii Kyrychenko](https://github.com/okyrychenko-dev)
